class Principe {

  contructor() {
    this.posX = 100;
    this.posY = 100;
    this.miColor = color(0, 0, 255);
  }

  dibujar() {
    fill(0);
    rect(this.posX, this.posY, 100, 200);
  }

  moverDerecha() {
  }

  moverIzquierda() {
  }

  estaVivo() {
  }
}
